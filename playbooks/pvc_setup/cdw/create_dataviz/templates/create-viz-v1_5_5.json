{
    "vizName":"{{ viz.name }}",
    "resourceTemplate": "{{ viz_template_id | default('viz-low') }}",
    "resourcePool": "{{ viz.queue | default('root.default') }}",
    "userGroups":["{{ viz.user_group | default('admin_group') }}"],
    "adminGroups":["{{ viz.admin_group | default('admin_group') }}"],
    "tags":[]
}
